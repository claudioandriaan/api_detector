services:
  - type: web
    name: api-detector
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      python -m playwright install chromium

    startCommand: |
      python -m playwright install chromium && streamlit run app.py --server.port=10000 --server.address=0.0.0.0